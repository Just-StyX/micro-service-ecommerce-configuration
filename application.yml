eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://localhost:8071/eureka
name:
  value: customer

#spring:
#  cloud:
#    config:
#      override-system-properties: false