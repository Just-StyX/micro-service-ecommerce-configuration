spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/product
    password: password
    username: username
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        boot:
          allow_jdbc_metadata_access: false


server:
  port: 8073